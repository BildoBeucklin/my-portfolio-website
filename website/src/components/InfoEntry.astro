---
const { title, subtitle, date, description } = Astro.props;
---

<div class="mb-8 last:mb-0">
  <div class="flex flex-col gap-y-1 md:flex-row md:justify-between md:items-baseline">
    <h3 class="text-lg font-semibold text-zinc-100">{title}</h3>
    <p class="text-sm text-zinc-400">{date}</p>
  </div>
  <h4 class="text-md text-zinc-300 mb-2">{subtitle}</h4>
  {description && <p class="text-sm text-zinc-400 text-pretty">{description}</p>}
</div>